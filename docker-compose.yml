services:
  transcribe:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - transcribe_tmp:/tmp/transcribe
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 4G

volumes:
  transcribe_tmp:
